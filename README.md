# PDF RAG 챗봇

내 PDF 문서에 질문하면, 문서 내용을 근거로 답해주는 RAG 챗봇입니다.
터미널 버전과 Streamlit 웹 앱 버전으로 구현했으며, 웹으로 배포했습니다.

**🔗 데모: https://easy-rag-chatbot-7kxhmutxqnbqjv8nyhfxz8.streamlit.app/**

## 사용 기술
- Python
- Google Gemini API (임베딩 모델 + 생성 모델)
- 코사인 유사도 기반 의미 검색
- Streamlit (웹 UI + Streamlit Community Cloud 배포)

## 작동 방식

1. **PDF 업로드** — 사용자가 웹에서 PDF를 업로드하면, PdfReader로
   읽어 하나의 문자열로 저장합니다.
2. **청킹** — 문자열을 문장 경계(목표 500자) 기준으로 잘라
   청크 리스트로 만듭니다.
3. **임베딩** — 청크들을 Gemini 임베딩 모델로 변환합니다.
   (API가 한 번에 100개까지만 처리해서, 100개씩 나눠 전송합니다.)
4. **검색** — 사용자 질문도 임베딩한 뒤, 각 청크와의 코사인 유사도를
   계산합니다. (유사도, 청크) 튜플로 저장하고, 유사도가 임계값(0.60)
   이상인 청크를 선별합니다.
5. **답변 생성** — 선별된 청크를 맥락으로 프롬프트에 넣어,
   Gemini 생성 모델로 답변을 만듭니다.

전체 흐름: PDF → 글자 → 청킹 → 임베딩 → 검색 → 답변 생성

웹 앱 버전에서는 PDF 읽기·청킹·임베딩을 캐싱(@st.cache_data)하여,
질문할 때마다 반복하지 않고 한 번만 실행하도록 최적화했습니다.

## 실행 방법

**배포된 버전은 위 데모 링크에서 바로 사용할 수 있습니다.**

로컬에서 실행하려면:
1. 필요한 라이브러리 설치:
   `pip install -r requirements.txt`
2. `.env` 파일에 `GEMINI_API_KEY=발급받은키` 작성
3. 실행:
   - 터미널 버전: `python rag.py`
   - 웹 앱 버전: `streamlit run app.py`


## 배운 점
- **임베딩**: 텍스트를 의미가 담긴 숫자 벡터로 바꾸는 것. 길이는 내용과
  무관하게 모델이 정한 고정값(3072)이고, 의미는 숫자 값에 반영된다.
- **코사인 유사도**: 두 벡터의 길이가 아니라 방향(각도)으로 의미의
  유사성을 잰다. 1에 가까울수록 비슷하고, numpy로 계산했다.
- **배치 처리**: API가 한 번에 100개까지만 받아서, 청크를 100개씩 나눠
  전송했다. 결과를 줄이는 게 아니라 전송을 쪼개는 것.
- **환각 방지**: 프롬프트에 "문서에 없으면 모른다고 답하라"는 지시와
  관련 맥락을 함께 넣어, 모델이 지어내지 않게 했다.
- **환경변수 관리**: API 키를 코드에 직접 쓰지 않고 .env에 분리해
  load_dotenv로 불러왔다. .gitignore로 키 노출도 방지했다.
- **캐싱**: Streamlit은 입력마다 코드를 처음부터 다시 실행하는데,
  무거운 임베딩 작업을 @st.cache_data로 한 번만 실행하고 결과를
  저장해 성능을 높였다.
- **청킹 전략**: 단순히 글자 수로 자르면 문장 경계에서 의미가 잘린다.
  문장 단위로 자르면 각 청크의 의미가 온전해지고, 검색 점수도 더
  뚜렷하게 갈린다.
- **임계값 설정**: 임베딩 유사도는 절대값이 아니라 상대적 분포로
  판단해야 한다. 무관한 텍스트도 유사도가 0이 아니라 0.55 정도로
  나오기 때문에, 관련/무관 경우의 점수를 비교해 그 사이를
  임계값으로 잡는다.
- **개선의 연쇄성**: 청킹 방식을 바꾸면 유사도 점수 분포가 달라져
  임계값도 다시 조정해야 한다. RAG의 파라미터(청크 크기, 임계값,
  top_k)는 서로 얽혀 있어 함께 튜닝해야 한다.

## 개선 이력
- **오버랩 청킹 적용 → 문장 단위 청킹으로 발전**: 처음엔 청크를 겹치게
  잘라 경계 손실을 줄였고, 이후 문장 경계 기준 청킹으로 대체해
  더 근본적으로 개선했습니다.
- **파일 업로드 기능**: 고정된 파일 대신 사용자가 직접 PDF를 업로드해
  질문할 수 있도록 개선했습니다.
- **웹 배포**: Streamlit Community Cloud에 배포하여 누구나 접속해
  사용할 수 있게 했습니다.
- **유사도 임계값 기반 검색**: 상위 N개를 무조건 가져오는 대신,
  코사인 유사도가 임계값(0.65) 이상인 청크만 선별하도록 개선했습니다.
  관련 있는 질문(유사도 0.7대)과 무관한 질문(0.55대)의 점수 분포를
  비교해 임계값을 결정했습니다.
- **문장 단위 청킹**: 글자 수로 무조건 자르던 방식(500자)을 문장 경계
  기준(목표 500자)으로 바꿔, 문장이 중간에 잘리는 문제를 해결했습니다.
  그 결과 관련/무관 질문의 점수 구분이 더 뚜렷해졌고, 이에 맞춰
  임계값을 0.65에서 0.60으로 재조정했습니다.

## 향후 개선 방향
- 여러 PDF 동시 처리
- 검색 품질 평가(evaluation) 체계 마련